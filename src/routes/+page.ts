// import { defaultConfiguration } from '$lib/canvas/canvas.js'
// import { stringToConfig } from '$lib/canvas/configConversion.js'

// /** @type {import('./$types').PageLoad} */
// export function load({ url }) {
// 	const configString = url.searchParams.get('config')
// 	if (configString) {
// 		const config = stringToConfig(configString)
// 		return { config: config }
// 	}
// 	return { config: defaultConfiguration() }
// }
